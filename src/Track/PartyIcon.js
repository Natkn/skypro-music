// PartyIcon.js (оставляем как есть)
import React from 'react';
import { ReactComponent as PartySvg } from '../../public/image/icon/party.svg';

const PartyIcon = (props) => {
  return <PartySvg {...props} />;
};

export default PartyIcon;
